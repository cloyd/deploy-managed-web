// React
import React from 'react';
import ReactDOM from 'react-dom';

import { AppWithProvider } from '../containers';

// Attach React to the DOM
ReactDOM.render(<AppWithProvider />, document.getElementById('App'));
